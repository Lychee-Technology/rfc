{
  "name": "ltbase-rfc",
  "private": true,
  "description": "Lint tooling for the LTBase RFC repository (custom markdownlint rules, see CONTRIBUTING.md)",
  "scripts": {
    "lint": "markdownlint-cli2",
    "test": "node --test \".markdownlint/test/*.test.mjs\""
  },
  "devDependencies": {
    "markdownlint": "^0.41.1",
    "markdownlint-cli2": "^0.23.2"
  }
}
